# Dashboard URLs removed - using API-only backend
# All dashboard functionality is handled by React frontend via API endpoints

urlpatterns = [
    # No URL patterns needed - all functionality moved to API
]
