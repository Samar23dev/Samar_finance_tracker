# Transaction URLs removed - using API-only backend
# All transaction functionality handled via API endpoints:
# - /api/transactions/ (CRUD operations)
# - /api/categories/ (CRUD operations)
# - /api/budgets/ (CRUD operations)

urlpatterns = [
    # No URL patterns needed - all functionality moved to API
]
