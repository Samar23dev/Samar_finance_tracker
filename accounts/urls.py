# Account URLs removed - using API-only backend
# Authentication handled via:
# 1. /api/auth/login/ (Token authentication)
# 2. /auth/ (Django Allauth OAuth)

urlpatterns = [
    # No URL patterns needed - all functionality moved to API
]
